{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "yarn workspace @dev-sandbox/server build",
    "dev": "yarn workspace @dev-sandbox/server dev",
    "test": "lerna run test",
    "clean": "rimraf packages/**/lib"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "volta": {
    "node": "14.17.4",
    "yarn": "1.22.5"
  }
}
